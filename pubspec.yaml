name: http_session
description: Easier to manage the sessions when using http to requests.
version: 0.0.2
homepage: https://github.com/vursin/http_session

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  http: ^0.13.4

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.3
