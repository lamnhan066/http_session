name: http_session
description: This plugin helps you easier to manage the sessions when using http to requests.
version: 0.2.2
homepage: https://github.com/lamnhan066/http_session

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  http: ^1.1.0
  cookie_store: ^0.3.5

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.3
