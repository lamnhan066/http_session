name: http_session
description: This plugin helps you easier to manage the sessions when using http to requests.
version: 0.2.0-rc.3
homepage: https://github.com/vnniz/http_session

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.1.0
  meta: ^1.11.0
  punycode: 1.0.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.3
